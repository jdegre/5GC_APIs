openapi: 3.0.0

info:
  version: 1.0.0
  title: A
  description: A

paths: {}

components:
  schemas:
    A:
      type: object
      properties:
        p1:
          $ref: 'b.yaml#/components/schemas/B3'
