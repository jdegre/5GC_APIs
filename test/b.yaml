openapi: 3.0.0
info:
  version: '-'
  title: 'Test B'
  description: 'Test B'

paths:
  /b:
    get:
      responses:
        '200':
          description: Expected response to a valid request
          content:
            application/json:
              schema:
                type: string

