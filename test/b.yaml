openapi: 3.0.0
info:
  version: '-'
  title: 'Test B'
  description: 'Test B'

paths:
  /a:
    get:
      responses:
        '200':
          description: Expected response to a valid request
          content:
            application/json:
              schema:
                type: string
  /a/{b}:
    get:
      parameters:
        - name: b
          in: path
          description: b
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Expected response to a valid request
          content:
            application/json:
              schema:
                type: string
  /a/b/{c}:
    get:
      parameters:
        - name: c
          in: path
          description: c
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Expected response to a valid request
          content:
            application/json:
              schema:
                type: string
